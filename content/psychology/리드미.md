# Shadow J — Origin Repository (Checksum Reference)

This repository is the **canonical origin reference**  
for the Shadow J independent website system.

---

## What This Repository Is

- This GitHub repository defines the **original state** of the project.
- A **specific commit hash** in this repository is the authoritative reference.
- Any distributed ZIP package is considered authentic **only if it matches this commit state**.

This repository itself is **not a blockchain**  
and does **not store checksum values as files**.

Instead, the repository and its commit history function as a **structural checksum reference**.

---

## What “Checksum” Means Here

- The integrity of the distributed ZIP file is verified using:
  - A SHA-256 hash of the ZIP file
  - This repository’s commit hash as the structural reference

If the ZIP file contents differ by even one character,  
the SHA-256 hash will not match.

---

## Timestamp & Proof of Existence (Optional but Verifiable)

- The SHA-256 hash of the original ZIP package  
  may be registered via **OpenTimestamp**.
- This provides cryptographic proof that the content existed  
  at a specific point in time.

The blockchain timestamp proves **existence and timing**,  
not authorship by itself.

---

## How Buyers Verify Authenticity

1. Download the provided ZIP package
2. Calculate its SHA-256 hash
3. Compare it with the hash provided by the author
4. Confirm that the structure matches this repository’s reference commit
5. (Optional) Verify the OpenTimestamp proof

---

## Authorship

This project was originally authored by **Shadow J**.

This repository exists solely to:
- Define the origin
- Enable verification
- Prevent false claims of originality


[방법 A: 폴더 뼈대 만들기(가장 안전)]
- Add file → Create new file
- filename에 아래처럼 입력:
  layouts/.gitkeep
- 내용은 아무거나 1줄:
  keep
- Commit message: "Create layouts skeleton"



Commit message: "Create layouts skeleton"
[2단계: (필수) 테마/레이아웃부터 올리기: layouts → assets → static]
※ “화면이 제대로 뜨는 핵심”은 layouts/assets/static 임
※ 이 3개가 먼저 들어가야 미리보기에서 즉시 확인 가능

2-1) layouts 업로드
   GitHub 웹에서:
   - Add file → Upload files
   - 업로드 화면에서 “파일들이 들어갈 경로”를 맞춰야 함
   - 방법 A(권장): 업로드 화면에서 “Create a new file”로 폴더 뼈대 만든 뒤 업로드

[방법 A: 폴더 뼈대 만들기(가장 안전)]
- Add file → Create new file
- filename에 아래처럼 입력:
  layouts/.gitkeep
- 내용은 아무거나 1줄:
  keep
- Commit message: Create layouts skeleton

이후:
- layouts 폴더 경로로 들어가서(Add file → Upload files)
- 압축해제 폴더의 layouts 안 “모든 파일” 업로드
- 업로드가 끝나면 기존 .gitkeep 파일은 나중에 삭제(선택)

2-2) assets 업로드 (같은 방식)
- Create new file:
  assets/.gitkeep
  내용: keep

  Create assets skeleton



- assets/ 안의 파일들 전부 업로드

2-3) static 업로드 (같은 방식)
- Create new file:
  static/.gitkeep
  내용: keep
  Commit: 
- 
- 
- Create static skeleton"
- static/ 안의 파일들 전부 업로드


[3단계: content 업로드 (글/섹션)]
※ content는 “글”이라서 나중에 교체/삭제가 많음
※ 체크섬 원본이면: 테스트 글은 제외하고 템플릿만 넣는 것을 권장(너가 말한 방식)

3-1) content 뼈대
- Create new file:
  content/.gitkeep
  내용: keep

  Create content skeleton

3-2) content 업로드
- 완성본 그대로 이식하려면: content/** 전부 업로드
- 체크섬용(깨끗한 원본)이라면:
  - 테스트 글(실험 글) 폴더/파일은 업로드하지 않음
  - 대신 “템플릿(예: _index.md / section index)”만 업로드
  - 또는 아예 content는 최소만 두고 나중에 템플릿만 추가

[4단계: (있으면) index.json 생성 방식 관련 파일 업로드]
너 검색이 /index.json을 fetch함.
즉 아래 중 “어떤 방식으로 index.json이 생성되는지”에 따라 파일이 필요함.

A) Hugo 출력에 index.json이 생성되도록 layouts에서 처리했다면:
- layouts 업로드만으로 끝(추가 작업 없음)

B) 별도 스크립트(예: node)로 index.json을 만든다면:
- package.json + scripts + 관련 파일(예: scripts/xxx.js)도 반드시 업로드 필요
- (있으면) tools/ or scripts/ 폴더도 같이 업로드


[5단계: 업로드가 끝났는지 “빠짐 체크”하는 법(폰에서도 가능)]
GitHub 레포에서 최상단 트리 기준으로 아래 5개가 보여야 함:

- hugo.toml (또는 config.*)
- layouts/
- assets/
- static/
- content/

그리고 너 프로젝트 기준으로는 보통 추가로:
- themes/ (사용하면)
- data/ (사용하면)
- i18n/ (사용하면)
- archetypes/ (사용하면)

※ “파일만 있고 폴더가 안 보인다” = 업로드가 루트로 잘못 들어간 것
   (예: layouts 파일들이 루트에 풀려버린 상태)
[6단계: Cloudflare Pages 연결(최소 설정)]
- Framework preset: Hugo (선택해도 되고, 안해도 됨)
- Build command: hugo
- Build output directory: public
- Environment variables:
  HUGO_VERSION = (너가 쓰던 버전이 있으면 그 버전)
  (없으면 비워도 되지만, 버전 고정이 안정적)

※ “hugo --minify” 같은 꼬리표는 선택사항
   안정성 최우선이면 "hugo" 단일로 간다.
[7단계: (선택) .gitkeep 정리]
- 폴더가 실제 파일로 채워졌으면 .gitkeep은 삭제해도 됨
- 삭제 커밋메시지 예시:
  "Remove skeleton placeholders"
[가장 흔한 실수 3개]
1) layouts 안 파일들이 루트로 업로드됨 → 화면/헤더/JS 다 깨짐
2) assets/static 빠짐 → 이미지/CSS/JS 경로 문제 발생
3) index.json 생성 로직 파일 빠짐 → 검색만 죽음(나머지 정상)
[네가 지금 “한 단계씩” 그대로 따라할 최소 순서(요약)]
4) hugo.toml 먼저 업로드 + 커밋
5) layouts/.gitkeep 만들기 + 커밋
6) layouts 폴더로 들어가 layouts 전체 업로드 + 커밋
7) assets/.gitkeep 만들기 + 커밋
8) assets 전체 업로드 + 커밋
9) static/.gitkeep 만들기 + 커밋
10) static 전체 업로드 + 커밋
11) content는 마지막(완성본/체크섬 방침에 따라 선택) + 커밋
12) Cloudflare 연결 → Preview 확인
13) 도메인 연결 → 최종 확인
좋다.  
아래는 **“압축파일 수동 이식”을 위해 뼈대만 먼저 만드는 단계**를  
👉 **GitHub 웹 UI에서 ‘New file’ → 복사/붙여넣기 → Commit**  
이 순서만 반복하면 되도록 **완전 기계식 매뉴얼**로 정리한 것이다.

핵심 원칙부터 먼저 **팩트**로 고정한다.

---

## 🔴 팩트 0 (중요 · GitHub 공식 동작)

- GitHub는 **빈 폴더를 만들 수 없다**
- **파일이 있어야 폴더가 생성된다**
- 따라서 “뼈대 폴더”는  
    → **임시 파일 (.gitkeep 또는 placeholder.txt)** 로 만든다
- 이후 **실제 파일 업로드가 끝나면 임시 파일은 삭제 가능**
- **같은 경로 + 같은 파일명** 업로드 시  
    → GitHub는 “수정(Edit)”으로 처리함 (오류 아님)

즉,  
👉 **뼈대용 파일은 ‘폴더 생성용’으로만 쓰고 버리는 게 정답**

---

# 3️⃣ 압축파일 수동 이식 – 뼈대 생성 단계 (완전 자동화 매뉴얼)

아래는 **v10 완성본 기준**  
👉 **새 레포에서 반드시 먼저 만들어야 할 “새 파일 경로 + 내용” 전체 목록**

---

## 1️⃣ content 루트 뼈대

### 📄 파일 생성

**경로**

```
content/_index.md
```

**내용 (그대로 복사)**

```markdown
---
title: "Index"
draft: true
---
```

Commit message:

```
chore: init content root
```

---

## 2️⃣ content 섹션 폴더 뼈대 (각 섹션 공통 패턴)

아래 **모든 섹션은 동일한 구조**다.  
(내용은 동일, 경로만 다름)

---

### 2-1. beginning

**경로**

```
content/beginning/_index.md
```

**내용**

```markdown
---
title: "Beginning"
draft: true
---
```

Commit message:

```
chore: init beginning section
```

---

### 2-2. human-behavior

**경로**

```
content/human-behavior/_index.md
```

**내용**

```markdown
---
title: "Human Behavior"
draft: true
---
```

Commit message:

```
chore: init human-behavior section
```

---

### 2-3. list

**경로**

```
content/list/_index.md
```

**내용**

```markdown
---
title: "List"
draft: true
---
```

Commit message:

```
chore: init list section
```

---

### 2-4. notes-on-living

**경로**

```
content/notes-on-living/_index.md
```

**내용**

```markdown
---
title: "Notes on Living"
draft: true
---
```

Commit message:

```
chore: init notes-on-living section
```

---

### 2-5. notes-sparks

**경로**

```
content/notes-sparks/_index.md
```

**내용**

```markdown
---
title: "Notes & Sparks"
draft: true
---
```

Commit message:

```
chore: init notes-sparks section
```

---

### 2-6. power-system

**경로**

```
content/power-system/_index.md
```

**내용**

```markdown
---
title: "Power System"
draft: true
---
```

Commit message:

```
chore: init power-system section
```

---

### 2-7. reflections

**경로**

```
content/reflections/_index.md
```

**내용**

```markdown
---
title: "Reflections"
draft: true
---
```

Commit message:

```
chore: init reflections section
```

---

## 3️⃣ layouts 뼈대 (HTML 들어갈 자리만 확보)

### 3-1. layouts/_default

#### 📄 baseof.html

**경로**

```
layouts/_default/baseof.html
```

**내용**

```html
<!-- placeholder: base layout -->
```

Commit:

```
chore: init baseof.html placeholder
```

---

#### 📄 list.html

**경로**

```
layouts/_default/list.html
```

**내용**

```html
<!-- placeholder: list layout -->
```

Commit:

```
chore: init list.html placeholder
```

---

#### 📄 single.html

**경로**

```
layouts/_default/single.html
```

**내용**

```html
<!-- placeholder: single layout -->
```

Commit:

```
chore: init single.html placeholder
```

---

## 3-2 layouts/partials 뼈대

### 📄 index.html

**경로**

```
layouts/partials/index.html
```

**내용**

```html
<!-- placeholder: partials index -->
```

Commit:

```
chore: init partials
```

---

## 5️⃣ static 뼈대

### 5-1. static/css

**경로**

```
static/css/.gitkeep
```

**내용**

```text
placeholder
```

Commit:

```
chore: init static css
```

---

### 5-2. static/img

**경로**

```
static/img/.gitkeep
```

**내용**

```text
placeholder
```

Commit:

```
chore: init static img
```

---

### 5-3. static/js

**경로**

```
static/js/.gitkeep
```

**내용**

```text
placeholder
```

Commit:

```
chore: init static js
```

---

## 6️⃣ 루트 필수 파일

### 📄 hugo.toml

**경로**

```
hugo.toml
```

**내용**

```toml
baseURL = "/"
languageCode = "en-us"
title = "Hugo Site"
```

Commit:

```
chore: init hugo config
```

---

### 📄 README.md

**경로**

```
README.md
```

**내용**

```markdown
# Hugo Site

Initial scaffold for manual migration.
```

Commit:

```
docs: init readme
```

---

# ✅ 다음 단계 (중요)

이제 할 일은 **딱 하나**다.

```text
압축파일을 풀고
→ 각 파일을
→ 위에서 만든 동일한 경로에
→ "Upload files"로 업로드
```

업로드가 끝나면:

```text
.gitkeep
placeholder 파일 전부 삭제
```

👉 그러면 **v10 원본과 100% 동일한 레포 구조 완성**

---

## 🔒 이 방식의 의미 (팩트)

- Android / iOS / PC 전부 가능
- Git 모르는 일반 사용자도 가능
- “왜 안 되죠?” 발생 시  
    → **“이 파일을 안 올리셨습니다”** 로 팩트 대응 가능
- 판매용 템플릿으로 **최상급 안정성**

---

다음으로 할 수 있는 것:

1. **이걸 PDF/README 판매용 매뉴얼로 정리**
2. **구매자용 ‘체크리스트 10줄’ 축약본**
3. **Cloudflare Pages 연결까지 포함한 풀 패키지 문서**

다음 단계 번호만 말해라.