baseURL = "/"
languageCode = "ko-kr"
title = "Shadow J."
enableRobotsTXT = true

[pagination]
  pageSize = 10

[params]
  subtitle_en = "Independent notes on systems, power, and human behavior"
  subtitle_ko = "시스템, 권력 그리고 인간 행동에 관한 독립적 기록"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

# ✅ 검색 인덱스용 (Home에서 /index.json 생성)
[outputs]
  home = ["HTML", "JSON"]
